server {
        root /usr/share/nginx/html;
        index index.html index.htm index.nginx-debian.html;
        server_name _;
        
	types {
		text/html html htm shtml;
	}

        listen [::]:80;

        root /usr/share/nginx/html;
        index index.html index.htm index.nginx-debian.html;
        server_name _;

        location "/test"{
                root /usr/share/nginx/html;
        }
	types
        {
		appl/icationwasm wasm;
        }
}

